{
  "type": "object",
  "properties": {
    "package": {
      "type": "string",
      "description": "The module/package the add-on is imported as, i.e. the name of the add-on folder."
    },
    "name": {
      "type": "string",
      "description": "The name displayed in Anki's UI (e.g. in the add-on list)"
    },
    "mod": {
      "type": "number",
      "description": "The time the add-on was last modified (unix epoch)"
    },
    "conflicts": {
      "type": "array",
      "items": { "type": "string" },
      "description": "A list of other packages that conflict"
    },
    "min_point_version": {
      "type": "number",
      "description": "the minimum 2.1.x version this add-on supports"
    },
    "max_point_version": {
      "type": "number",
      "description": "If negative, abs(n) is the maximum 2.1.x version this add-on supports. If positive, indicates version tested on, and is ignored"
    },
    "branch_index": {
      "type": "number",
      "description": "AnkiWeb sends this to indicate which branch the user downloaded"
    },
    "human_version": {
      "type": "string",
      "description": "Human-readable version string set by the add-on creator"
    },
    "version": {
      "type": "string",
      "description": "[aab] Version string, set to the same value as human_version"
    },
    "ankiweb_id": {
      "type": "string",
      "description": "[aab] The AnkiWeb upload ID."
    },
    "author": {
      "type": "string",
      "description": "[aab] The main author/maintainer/publisher of the add-on."
    },
    "homepage": {
      "type": "string",
      "description": "Homepage of the add-on project (e.g. GitHub repository link)"
    }
  },
  "required": ["package", "name"]
}
